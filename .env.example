NODE_ENV=development
PORT=4000
DATABASE_URL=postgresql://user:password@host:5432/dbname?sslmode=require

# Auth: use a long random string in production (e.g. openssl rand -base64 32)
JWT_SECRET=your-secret-at-least-16-characters-long
# Access token lifetime (e.g. 15m, 1h). Short is safer; refresh token extends the session.
JWT_ACCESS_EXPIRY=15m
# Refresh token lifetime (e.g. 7d). Used to get new access tokens without re-login.
JWT_REFRESH_EXPIRY=7d

# Optional: required for Google sign-in. Use the same OAuth 2.0 Client ID as in the web app (NEXT_PUBLIC_GOOGLE_CLIENT_ID).
# GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
